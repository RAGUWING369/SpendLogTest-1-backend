# Development and testing dependencies.
#
# Usage:
#   Local dev / CI test step:  pip install -r requirements-dev.txt
#   Production container only: pip install -r requirements.txt
#
# This file extends requirements.txt — every production dependency is included
# automatically via the -r directive below.
-r requirements.txt

# ── HTTP Client ────────────────────────────────────────────────────────────────
# httpx.AsyncClient is used as the async test transport for FastAPI endpoints
# (passed as the transport argument to TestClient / httpx.AsyncClient in tests).
# Not required at runtime in production.
httpx==0.27.2

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==0.23.8
pytest-cov==5.0.0

# ── Code Quality ──────────────────────────────────────────────────────────────
black==24.8.0
isort==5.13.2
