"""Pydantic v2 request/response schema sub-package.

Schemas define the public API contract — validated input shapes and
serialised output shapes.  They are intentionally decoupled from ORM models.
"""
